// Encapsulates a detection of digits within an image,
// e.g. by a service such as Google Vision API

message {
  // must match the timestamp of the request
  long timestamp;
  // Copied from request
  string image_info;

  repeated string strings;
  repeated IRect boxes;

  string exception;
  // If there was an exception, this will be its stack trace
  repeated string stack_trace;

}
