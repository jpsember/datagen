// A request to be made of a service (e.g. Google Vision API)

message {
  long timestamp;
  bytes image;

  // For debugging, e.g. a filename
  string image_info;

  // True if the request is being made from a simulated source
  bool simulated;
}
